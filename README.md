# Birthday Site for ememzeze

This repository contains the standalone birthday website.

## Hosting

This site is intended to be deployed as a static website on GitHub Pages.

## Notes

- `index.html`, `styles.css`, and `script.js` are the deployed files.
- Uploaded photos and letters are stored in the visitor's browser using `localStorage`.
- This repository is intentionally separate from `equation-quest`.
